let numbers = [1,2,3,4,5,6,7];
function reverse(array){
    if(!Array.isArray(array)){console.log("Enter an array!")}else{
    for(let i = array.length; i >= 0; i--){
        console.log(array[i]);
    }
}
}

// reverse(numbers);
reverse("yea");